// import store from "../../redux/store";

// export default function handler(req, res) {
//   const state = store.getState();
//   const mode = state.mode; // Access your mode state here
//   res.status(200).json({ mode });
// }
